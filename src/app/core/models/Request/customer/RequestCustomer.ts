import { TypeDocument } from '../../enum/TypeDocument';

export class RequestCustomer {
    name: string;
    typeDocument: TypeDocument;
    nroDocument: string;
    email: string;
    address: string;
    phone: string;
}
